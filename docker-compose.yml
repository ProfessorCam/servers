services:
  server-basics:
    build: .
    container_name: server-basics
    # Plain port mapping: this site has no live-network section, so it does
    # not need the host's interfaces. The site listens on 8083 inside and out.
    ports:
      - "8083:8083"
    restart: unless-stopped
